from soz_analizi.shekilci import *
from soz_analizi.sekilci.stringler import *

from soz_analizi.sekilci.n_isim import *
from soz_analizi.sekilci.n_feil import *
from soz_analizi.sekilci.n_sifet import *
from soz_analizi.sekilci.n_say import *
from soz_analizi.sekilci.n_evezlik import *
from soz_analizi.sekilci.n_zerf import *
from soz_analizi.sekilci.n_SpecificNoun import *
from soz_analizi.sekilci.isim_animals import *
sait=['a', 'ı', 'o', 'u','e', 'ə' , 'i', 'ö', 'ü']
istisna=['tale', 'mənbə', 'mövqe', 'mənşə', 'nə', 'mənafe']
istisna1=['isim', 'qisim', 'nəsil', 'fikir', 'ətir', 'səbir', 'qəbir','heyif','sətir','qədir','ömür', 'sinif',
 'eyib', 'şəkil', 'meyil', 'əsil', 'fəsil', 'xeyir', 'zehin']
istisna2=['beyin','ağıl','alın','burun','ağız','boyun','qoyun','çiyin','oğul','qarın','əyin','qayın']   #qoyun is omonim both versions should work but do not know how

#################################################
#############
